# Notes Sandbox Plugin

This example registers `plugin:example.notes`, a minimal sandbox edit that stores a note for a node under `SimulationSnapshot.meta.notes`.

It demonstrates the public sandbox plugin API:

- `PluginEditSpec`
- `registerSandboxEdit`
- plugin validator and serializer
- pure reducer behavior
- `testPlugin` compatibility
